{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm run db:deploy && node -e \"const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.company.deleteMany().then(()=>p.$disconnect())\" && npm run db:seed && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
